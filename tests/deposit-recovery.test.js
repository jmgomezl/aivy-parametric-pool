import test from 'node:test';import assert from 'node:assert/strict';import{verifyDepositReceipt}from'../src/demo/reconcile.js';
const terms={transactionId:'0.0.1@100.000000001',assetId:'0.0.2',shareId:'0.0.3',poolId:'0.0.4',treasuryId:'0.0.1',accountId:'0.0.5',amount:25};
const fixture=()=>({transaction_id:'0.0.1-100-000000001',result:'SUCCESS',name:'CRYPTOTRANSFER',scheduled:false,nonce:0,nft_transfers:[],token_transfers:[['0.0.2','0.0.5',-25e6],['0.0.2','0.0.4',25e6],['0.0.3','0.0.1',-25e8],['0.0.3','0.0.5',25e8]].map(([token_id,account,amount])=>({token_id,account,amount,is_approval:false}))});
test('only the exact successful atomic deposit reconciles a lost response',()=>{assert.equal(verifyDepositReceipt([fixture()],terms).shares,25);for(const patch of[{result:'FAIL_INVALID'},{transaction_id:'other'},{scheduled:true},{nonce:1}])assert.throws(()=>verifyDepositReceipt([{...fixture(),...patch}],terms));const wrong=fixture();wrong.token_transfers[3].account='0.0.9';assert.throws(()=>verifyDepositReceipt([wrong],terms));const short=fixture();short.token_transfers.pop();assert.throws(()=>verifyDepositReceipt([short],terms));});
